import { Client } from "./client";

export class Beneficaire {

    idBeneficiaire  ? : number ;
    nomClient ? : String;
    preomClient ? : String;
    telClient ? : number;
    clientid ? : Client;
}
