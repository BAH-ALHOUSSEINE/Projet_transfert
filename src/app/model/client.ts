export class Client {
        idClient? : number;
        nomClient ? : String;
        prenomClient ? : String;
        telClient ? : number;
        emailClient  ? : String;
        dateNaissance ? : Date;
        passwordClient ?  : String;
        passwordconf ?  : String;
}
